import ExitBackButton from "../components/exit-back-button";
import WeatherWidget from "../components/weather-widget";

const WeatherPage = () => (
  <>
    <ExitBackButton title="Погода" />
    <WeatherWidget />
  </>
);

export default WeatherPage;
